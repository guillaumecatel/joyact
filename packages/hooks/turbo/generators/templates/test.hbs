import { {{ camelCase name }} } from './{{ camelCase name }}'

describe('Hooks / {{ camelCase name }}', () => {
  it('should return Hello world', () => {
    const [message] = {{ camelCase name }}()
    expect(message).toBe('Hello world')
  })
})
