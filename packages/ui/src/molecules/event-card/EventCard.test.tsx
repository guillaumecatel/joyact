describe('Molecules / EventCard', () => {
  it('should match snapshot for the EventCard component', () => {
    // const component = render(<EventCard />)
    // expect(component).toMatchSnapshot()

    expect(true).toBe(true)
  })
})
